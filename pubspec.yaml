name: chromatic_chasm
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_joystick: ^0.2.1
  flutter_localizations:
    sdk: flutter
  intl: any
  lzstring: ^2.0.0+2
  provider: ^6.1.2
  rive: ^0.13.20
  shared_preferences: ^2.5.2
  vector_math: ^2.1.4

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/background.jpeg
    - assets/arcade_controls.riv
